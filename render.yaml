services:
  - type: web
    name: draw-app-ws-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: / 
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
